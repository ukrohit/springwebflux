### Unit testing in the reactive programming 

    - StepVerifier.create()
    - expect
      expectNext
      expectNextCount()
      expectConsumeWhile
      assertNext
    - Complete/error
        expectComplete
        expectError
    - verify()

### Show the sql query for the ractiveRepository 
    logging.level.org.springframework.r2dbc = DEBUG 

